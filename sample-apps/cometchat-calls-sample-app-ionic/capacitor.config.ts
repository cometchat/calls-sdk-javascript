import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cometchat-calls-sample-app-ionic',
  webDir: 'dist'
};

export default config;
